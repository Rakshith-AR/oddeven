#include <stdio.h>

int main() {
    int num;

    // Input from user
    printf("Enter an integer: ");
    scanf("%d", &num);
              print("yes")
    // Check if the number is even or odd
    if (num % 2 == 0) {
        printf("%d is even.\n"not odd num);
    } else {
        printf("%d is odd.\n" not even, num);
    }
        print("no")
    return 0;
}

